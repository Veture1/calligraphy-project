# v0.2.3 Implementation Plan

> **Goal**: Agent Awareness & Native Module Fix
> **Epic**: `compound_agent-crq`
> **Issues**: 14 total (1 P0, 6 P1, 7 P2)

---

## Phase Overview

| Phase | Focus | Issues | Est. Tests |
|-------|-------|--------|------------|
| **1** | Critical Bug Fix | 1 | 5-10 |
| **2** | Core CLI Features | 3 | 15-20 |
| **3** | Claude Integration | 4 | 10-15 |
| **4** | Polish & Cleanup | 6 | 10-15 |

---

## Phase 1: Critical Bug Fix (P0)

### `2f0` - Fix better-sqlite3 native bindings error

**Problem**: better-sqlite3 fails when compound-agent is installed as a dependency in another project.

**Approach**: Option D - Lazy-load SQLite with graceful degradation

```typescript
// Graceful degradation - JSONL still works without SQLite
let db: Database | null = null;
try {
  const Database = require('better-sqlite3');
  db = new Database(path);
} catch (e) {
  console.warn('SQLite unavailable, using JSONL-only mode');
}
```

**Why this approach**:
- No postinstall scripts (fragile across platforms)
- No optional dependencies (confusing)
- JSONL is source of truth anyway
- SQLite is just a cache/index

**Tests**:
1. SQLite available → normal operation
2. SQLite unavailable → JSONL-only mode works
3. Search falls back to keyword search when no FTS
4. Stats shows degraded mode warning
5. Integration: install in fresh project, verify works

**Files**: `src/storage/sqlite.ts`, `src/storage/index.ts`

---

## Phase 2: Core CLI Features (P1)

### `gpv` - Add 'ca prime' command

**Purpose**: Context recovery after compaction/clear.

**Implementation**:
```typescript
// src/commands/management.ts
program.command('prime')
  .description('Output workflow context for Claude Code')
  .action(() => {
    // Output workflow rules, commands, quality gate
  });
```

**Tests**:
1. prime outputs workflow context
2. Output includes all critical rules
3. Output includes command reference
4. Output includes quality gate

**Files**: `src/commands/management.ts`

---

### `8lp` - Add /learn slash command

**Implementation**: Create `.claude/commands/learn.md` during `ca init`

```markdown
---
description: Capture a lesson from this session
arguments: insight
---
ca learn "$ARGUMENTS"
```

**Tests**:
1. `ca init` creates commands directory
2. `learn.md` file has correct content
3. Other slash commands created (list, show, etc.)

**Files**: `src/commands/setup.ts`

---

### `6nw` - Fix plan-time lessons (check-plan not hooked)

**Implementation**:
1. Add instructions in AGENTS.md template
2. Create `/check-plan` slash command

**Tests**:
1. AGENTS.md includes plan-time retrieval instructions
2. check-plan.md slash command created
3. Instructions are clear and actionable

**Files**: `src/commands/setup.ts`

---

## Phase 3: Claude Integration (P1-P2)

### `0p5` - Fix Claude editing JSONL directly

**Implementation**: Update AGENTS.md template with explicit prohibition

```markdown
## CRITICAL RULES
**NEVER edit .claude/lessons/index.jsonl directly.**
Always use: `ca learn`, `ca update`, `ca delete`
```

**Tests**:
1. AGENTS.md includes "NEVER edit" rule
2. Rule is prominent (near top)
3. Lists all CLI alternatives

**Files**: `src/commands/setup.ts`

---

### `ctv` - Create Claude Plugin structure

**Implementation**: Create plugin manifest and commands

```
.claude-plugin/
├── plugin.json
└── commands/
    ├── learn.md
    ├── list.md
    ├── check-plan.md
    ├── prime.md
    └── ...
```

**Depends on**: `8lp`, `gpv`

**Tests**:
1. Plugin structure created during init
2. plugin.json has correct hooks
3. All commands reference correct CLI

**Files**: `src/commands/setup.ts`, new template files

---

### `501` - Wire detection triggers to workflow

**Implementation**: Add self-detection instructions to AGENTS.md

```markdown
## When to Capture Lessons
Watch for these patterns and propose `ca learn`:
- User correction: "no", "wrong", "actually..."
- Self-correction: you fix after multiple attempts
- Test failure: test fails → you fix → passes
```

**Tests**:
1. AGENTS.md includes trigger patterns
2. Instructions are actionable

**Files**: `src/commands/setup.ts`

---

### `2jp` - Add auto-invoke triggers

**Implementation**: Document trigger phrases in AGENTS.md

**Tests**:
1. Trigger phrases documented
2. Action for each trigger clear

**Files**: `src/commands/setup.ts`

---

### `lfy` - Add CLAUDE.md reference to AGENTS.md

**Implementation**: During init, add reference with markers

```markdown
<!-- compound-agent:claude-ref:start -->
## Compound Agent
See @AGENTS.MD for lesson capture workflow.
<!-- compound-agent:claude-ref:end -->
```

**Tests**:
1. CLAUDE.md created if missing
2. Reference appended if exists
3. No duplicate if already present
4. Markers enable clean removal

**Files**: `src/commands/setup.ts`

---

## Phase 4: Polish & Cleanup (P2)

### `e2r` - Fix AGENTS.md not cleaned on uninstall

**Implementation**: Use markers, add removal logic

**Tests**:
1. Markers present after init
2. Section removed on uninstall
3. Other content preserved

**Files**: `src/commands/setup.ts`

---

### `qi0` - Add 'setup claude --status' diagnostic

**Implementation**:
```bash
ca setup claude --status
# Output: settings file, hooks, test results
```

**Tests**:
1. Shows settings file location
2. Shows hook status
3. Shows actionable fix if incomplete
4. --json output works

**Files**: `src/commands/setup.ts`

---

### `79k` - Ensure CLI releases resources on exit

**Implementation**: Register cleanup handlers

```typescript
process.on('exit', cleanup);
process.on('SIGINT', cleanup);
process.on('SIGTERM', cleanup);
```

**Tests**:
1. Cleanup called on normal exit
2. Cleanup called on SIGINT
3. Safe if resources never loaded

**Files**: `src/cli.ts`

---

### `k6x` - Documentation quality

**Implementation**: Review and improve all docs

**Checklist**:
- [ ] README.md - clear onboarding
- [ ] AGENTS.md - explicit usage
- [ ] CLI --help - useful examples
- [ ] Error messages - actionable

---

## Dependency Graph

```
Phase 1 (P0):
  2f0 (native bindings)
    ↓
Phase 2 (P1 features):
  gpv (prime) ──────┐
  8lp (/learn) ─────┼──→ ctv (plugin)
  6nw (check-plan) ─┘
    ↓
Phase 3 (integration):
  0p5 (no direct edit)
  501 (detection triggers)
  2jp (auto-invoke)
  lfy (CLAUDE.md ref)
    ↓
Phase 4 (polish):
  e2r (uninstall cleanup)
  qi0 (--status)
  79k (resource cleanup)
  k6x (docs)
```

---

## TDD Workflow (Per Issue)

For each issue:

```
1. bd update <id> --status=in_progress
2. /invariant-designer      → Define what must be true
3. /test-first-enforcer     → Write failing tests
4. /property-test-generator → Edge case tests
5. Implement minimal code
6. /anti-cargo-cult-reviewer → Verify tests are real
7. /module-boundary-reviewer → Validate design
8. /implementation-reviewer  → MUST return APPROVED
9. bd close <id>
```

---

## Success Criteria

v0.2.3 is complete when:

- [ ] All 14 issues closed
- [ ] `pnpm test` passes (100%)
- [ ] `pnpm lint` passes (0 violations)
- [ ] Fresh install in new project works
- [ ] Claude correctly uses CLI (not direct JSONL edit)
- [ ] /learn, /prime, /check-plan slash commands work
- [ ] Uninstall cleanly removes all artifacts
