# Dependencies
node_modules/

# Build output
go/dist/
dist/

# Cache (any nesting depth)
**/.claude/.cache/
.claude/.ca-*.json

# Setup-generated (ca setup dogfooding on source repo)
.claude/agents/compound/
.claude/commands/compound/
.claude/skills/compound/
.claude/plugin.json
.claude/settings.json
.claude/settings.local.json

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Beads runtime artifacts
.beads/dolt-monitor.pid
.beads/dolt-server.lock
.beads/dolt-server*.pid
.beads/dolt-server*.port

# Go build artifacts
go/ca

# Binary build output (keep bin/ca wrapper script)
bin/ca-binary
bin/ca-embed
bin/ca-test
bin/checksums.txt

# Nested .claude dirs from test runs / dogfooding
go/.claude/
go/internal/**/.claude/

# Rust build artifacts
rust/embed-daemon/target/

# Compound agent runtime artifacts (loop scripts + agent logs)
.compound-agent/

# Claude worktrees
.claude/worktrees/

# Worktree harvest snapshots (retained on failure; must not pollute git status)
.ca-worktree-snapshots/

# Environment
.env
.env.*

# Dolt database files (added by bd init)
.dolt/
*.db
*.db-shm
*.db-wal

# Release artifacts (CI only)
release-bins/
npm-tmp-*/
rust-artifacts/

# Beads / Dolt files (added by bd init)
.beads-credential-key
